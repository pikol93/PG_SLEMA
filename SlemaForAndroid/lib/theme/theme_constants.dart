import 'package:flutter/material.dart';

ThemeData lightTheme = ThemeData(
  useMaterial3: true,
  brightness: Brightness.light,
  colorSchemeSeed: const Color(0xffb6ddfa),
);

ThemeData darkTheme =
    ThemeData(useMaterial3: true, brightness: Brightness.dark);
